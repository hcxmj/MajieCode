# MajieCode 交互式对话终端 Spec

## 背景

MajieCode 是一个运行在终端里的 AI 编程助手（类 Claude Code），目标是从零构建。本阶段聚焦最核心的能力：一个交互式对话终端。用户启动后进入 TUI，输入问题，程序调用大模型 API 并把回复流式逐字打印，支持多轮上下文记忆。为便于日后接入更多后端与 agent 能力，供应商访问层需抽象为统一接口。本阶段不涉及工具调用、文件操作、代码编辑等 agent 功能。

## 目标

- 提供一个交互式终端对话界面，支持连续多轮对话，模型能记住本次会话的历史。
- 通过 YAML 配置文件管理多个 LLM 供应商，启动时按配置选择当前使用的供应商，支持临时切换后端。
- 大模型回复以流式（SSE）方式逐字增量输出，而非一次性返回。
- 抽象统一的供应商访问接口，屏蔽不同协议族（OpenAI 兼容 / Anthropic 原生）的差异，新增后端只需实现该接口。
- 支持 Anthropic Claude 的扩展思考（extended thinking），并在界面上实时、样式区分地展示思考过程。

## 功能需求

- F1 交互式终端界面：启动后进入 TUI，包含输入区与滚动的对话展示区，用户输入问题回车发送；程序运行期间可连续输入多轮。
- F2 流式增量输出：模型回复通过 SSE 流式接收，界面逐字/逐片段增量渲染，而非等待完整回复后一次性显示。
- F3 多轮上下文记忆：同一会话内，程序维护完整对话历史（含系统提示、用户消息、助手回复），每次请求携带历史，模型能引用之前的内容。
- F4 多供应商配置：从 YAML 配置文件读取多个供应商条目，每个条目含 `name`、`protocol`、`model`、`base_url`、`api_key`、`thinking`（可选）六个字段；顶层 `default` 字段指定启动时默认使用的供应商 `name`。
- F5 配置项选择与切换：启动时默认使用 `default` 指向的供应商；同时支持启动参数临时覆盖，指定使用哪个 `name` 的配置。
- F6 统一供应商接口：抽象统一的供应商访问接口，接收对话历史与参数，返回流式事件（文本增量、思考增量、结束、错误）；`protocol` 为 `openai` 或 `anthropic` 时分别由对应适配器实现，新增后端只需实现该接口。
- F7 扩展思考支持：当供应商配置 `thinking` 启用时，请求开启扩展思考；接收思考内容的流式增量，并在界面上以区别于正式回复的样式实时展示。
- F8 API Key 环境变量插值：`api_key` 字段支持 `${ENV_VAR}` 形式的环境变量插值，运行时替换为对应环境变量的值；也允许直接写明文。
- F9 控制命令：TUI 内识别 `/exit`（退出）、`/clear`（清空当前会话历史）、`/help`（显示帮助）三个控制命令，其余输入作为普通提问发送给模型。
- F10 错误处理：配置缺失/非法、API Key 未设置、网络或 API 调用失败时，在界面上给出清晰的错误提示，且不崩溃退出，可继续下一轮输入。

## 非功能需求

- N1 可扩展性：新增一个 LLM 后端时，若其协议属于已支持的族（openai/anthropic），仅需增加一条 YAML 配置即可使用；若为全新协议，只需实现统一供应商接口的一个适配器，不改动 TUI 与会话逻辑。
- N2 响应即时性：流式输出应在收到首个片段后尽快显示（首字延迟低），逐片段刷新，避免明显卡顿或整段一次性弹出。
- N3 健壮性：任何单次请求的异常（网络、鉴权、超时、非法响应）都被捕获并转为界面提示，不导致程序崩溃或会话丢失。
- N4 配置安全：支持通过环境变量插值引用 API Key，使密钥不必明文写入配置文件；配置文件本身不纳入版本控制。
- N5 中文优先：界面提示、帮助文本、错误信息以简洁中文呈现。
- N6 跨平台终端兼容：在常见的 macOS / Linux 终端下正常渲染与交互。
- N7 依赖清晰：使用 Python 实现，依赖通过依赖清单文件声明并可复现安装。

## 不做的事

- B1 不实现工具调用（tool use / function calling）、文件读写、代码编辑、命令执行等 agent 能力。
- B2 不做对话历史的磁盘持久化，退出即清空（仅当前会话内存保留）。
- B3 不在 TUI 内做供应商的运行时切换命令（如 `/model`）；切换供应商通过重启 + 配置/启动参数完成。
- B4 不实现多会话管理、会话分支、历史检索等高级会话功能。
- B5 不做 token 计数、费用统计、限流重试等运维类特性。
- B6 不实现图片、音频等多模态输入输出，仅纯文本对话。
- B7 不做配置文件的图形化/交互式编辑，配置由用户手动编写 YAML。
- B8 不为 OpenAI 兼容族之外、Anthropic 之外的私有协议单独适配（本阶段只交付这两族适配器）。

## 验收标准

- AC1（F1/F9）：启动程序后进入交互界面，能连续输入多轮；输入 `/help` 显示命令帮助，`/clear` 后历史被清空，`/exit` 正常退出。
- AC2（F2/N2）：向模型提问后，回复以逐字/逐片段方式增量出现在界面上，而非等待完整回复一次性显示。
- AC3（F3）：多轮对话中，第二轮提问引用第一轮内容（如「我刚才问了什么」），模型能正确回忆，证明历史被携带。
- AC4（F4/F5）：YAML 配置两个及以上供应商条目，不加参数启动时使用 `default` 指向的供应商；用启动参数指定另一 `name` 时，改用对应供应商（可通过界面显示的当前供应商/模型确认）。
- AC5（F6/N1）：`openai` 与 `anthropic` 两族各配置一个供应商，均能正常发起对话并流式返回；新增同族后端仅通过增加 YAML 配置即可使用。
- AC6（F7）：为支持扩展思考的 Claude 配置启用 `thinking` 后提问，思考过程以区别于正式回复的样式实时展示，随后给出正式回复。
- AC7（F8/N4）：`api_key` 写成 `${某环境变量}`，设置该环境变量后启动能正常鉴权调用；未设置时给出清晰的缺失提示。
- AC8（F10/N3）：故意配置错误的 base_url 或 api_key，界面显示清晰错误提示且不崩溃，仍可继续下一轮输入。
- AC9（N7）：按依赖清单在干净环境安装后可直接启动运行。
