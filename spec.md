# MajieCode 工具系统 Spec

## 背景

MajieCode 现为交互式对话终端：TUI 输入、流式回复、多轮记忆、双协议 Provider（openai/anthropic），但只能「动嘴」——无法读写文件、执行命令、检索代码。要成为真正能干活的 Agent，需要一套工具系统：模型在流式响应中声明要调用的工具及参数，宿主代码执行工具，再把结构化结果回灌进对话历史。

## 目标

- 定义统一的工具抽象：每个工具携带名称、描述、参数 Schema 与执行方法。
- 实现六个核心工具：读文件、写文件、改文件、执行命令、按模式找文件、搜代码内容。
- 提供工具注册中心：集中登记、按名查找、导出为两族 API 认得的工具列表。
- 工具执行具备超时与错误处理，失败信息包成结构化结果返回模型，让模型据此调整而非崩溃。
- 两族 Provider（openai/anthropic）均能解析流式工具调用（拼接 JSON 参数碎片），执行结果回灌进对话历史。
- 所有文件/命令操作限制在启动工作目录内，越界路径拒绝执行。
- 本章为单轮执行：模型发起工具调用 → 宿主执行 → 结果写回历史即停，不自动再请求模型（Agent Loop 留待下一章）。

## 功能需求

- F1 工具抽象接口：定义统一的工具接口，每个工具提供四项元信息——名称、面向模型的描述、参数 Schema（声明参数名、类型、是否必填），以及一个接收已解析参数、返回结构化结果的执行方法。
- F2 读文件工具：给定工作目录内的文件路径，返回文件文本内容；文件不存在或非文本时返回结构化错误。
- F3 写文件工具：给定路径与内容，创建或整体覆盖写入文件；必要的父目录自动创建；越界路径拒绝。
- F4 改文件工具：给定路径、原文片段、替换片段，在文件中做唯一匹配替换。原文恰好出现一次才替换成功；零次或多次匹配均返回清楚的错误（指明未匹配或匹配多处），供模型重试。
- F5 执行命令工具：在工作目录内执行 shell 命令，捕获标准输出、标准错误与退出码一并返回；执行超时则中止并返回超时错误。
- F6 按模式找文件工具：给定 glob 模式，返回工作目录内匹配的文件路径列表。
- F7 搜代码内容工具：给定正则/文本模式，在工作目录内检索文件内容，返回命中的文件、行号与行文本；可选按文件类型或路径范围收敛。
- F8 工具注册中心：集中登记所有工具，支持按名称查找并取得对应工具；能将全部工具的元信息导出为 openai 与 anthropic 两族 API 各自认得的工具声明列表。
- F9 工具调用解析：两族 Provider 在流式响应中识别工具调用，累积拼接分片到达的 JSON 参数碎片，流结束时产出完整的工具调用（含调用 id、工具名、参数对象）。
- F10 结构化执行结果：工具执行统一返回结构化结果，区分成功与失败，携带可读的输出或错误文本；失败不抛出到主循环，而是作为结果回灌给模型。
- F11 结果回灌历史：宿主执行完工具后，将「模型的工具调用」与「工具执行结果」按各协议要求追加进对话历史；本章追加后即停，不自动发起下一次模型请求。
- F12 越界与超时防护：所有涉及文件系统与命令执行的工具，对超出工作目录的路径一律拒绝并返回错误；命令执行受统一超时约束。

## 非功能需求

- N1 一致的抽象：新增工具只需实现统一工具接口并登记到注册中心，无需改动 Provider 或主循环。
- N2 协议无关的工具定义：工具自身不感知 openai/anthropic 差异；协议适配由 Provider 层与注册中心导出逻辑负责。
- N3 健壮性：任何单个工具的失败（异常、超时、越界、参数缺失）都被捕获为结构化错误，绝不使主循环崩溃。
- N4 零重型依赖：搜索与找文件基于 Python 标准库（os/glob/re）实现，不引入 ripgrep 等外部程序。
- N5 中文交互：工具执行的过程提示、错误信息面向用户的部分用中文呈现。
- N6 可观测：TUI 中能看到模型发起了哪个工具调用及其参数、以及工具返回结果的摘要。

## 不做的事

- 不做 Agent Loop：单轮执行后停，不自动把工具结果再喂给模型循环，不做多工具连环调用编排（留待下一章）。
- 不做命令执行确认交互：命令直接执行，不弹用户批准（靠工作目录限制与超时兜底）。
- 不做工具执行的持久化/日志文件：仅内存与终端展示。
- 不做并发/并行工具执行：一次一个工具，顺序执行。
- 不做工作目录之外的沙箱加固（如 chroot、权限降级）：仅做路径越界校验。
- 不做工具参数的复杂校验框架：仅按 Schema 做基本必填/类型检查。

## 验收标准

- AC1（F1/F8）：定义任一工具并登记后，可通过名称从注册中心取回该工具，并读到其名称、描述、参数 Schema。
- AC2（F8/N2）：注册中心能导出 openai 与 anthropic 两族各自格式的工具声明列表，字段结构符合对应 API 要求。
- AC3（F2）：对工作目录内存在的文本文件调用读文件返回完整内容；对不存在的路径返回结构化错误而非崩溃。
- AC4（F3）：写文件到工作目录内的新路径（含尚不存在的子目录），文件被创建且内容正确；覆盖写入生效。
- AC5（F4）：改文件在原文唯一匹配时替换成功；未出现时返回「未匹配」错误；出现多次时返回「匹配多处」错误，均不修改文件。
- AC6（F5）：执行命令返回标准输出、标准错误与退出码；对超时命令中止并返回超时错误。
- AC7（F6）：按 glob 模式找文件返回匹配路径集合；无匹配时返回空结果而非错误。
- AC8（F7）：搜代码内容按模式返回命中项，每项含文件路径、行号与行文本；无命中时返回空结果。
- AC9（F12）：对超出工作目录的路径调用任一文件/命令工具，均被拒绝并返回越界错误，未发生实际读写。
- AC10（F9）：两族 Provider 在收到分片的工具调用时能拼接 JSON 碎片，流结束后产出结构完整的工具调用。
- AC11（F10/N3）：工具执行失败时返回结构化失败结果，主循环不崩溃并继续可用。
- AC12（F11）：模型发起工具调用后，历史中依次出现「助手的工具调用」与「工具结果」；本轮追加后停止，未自动发起下一次请求。
- AC13（端到端）：在 tmux 启动 MajieCode，让模型读取某个真实文件，可观察到：模型发起读文件调用 → 宿主执行 → 结果回灌历史 → 本轮结束，全程无崩溃。
